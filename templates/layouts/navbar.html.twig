{% block stylesheets %}
<style>
  /* Set background color of navbar */
  .navbar {
    background-color: rgb(20, 97, 180);
    width: 100%;
    position: fixed;
    top:0px;
    z-index: 1;
    backface-visibility: hidden;
  }

  /* Set color of text in navbar */
  .navbar a {
    color: #0d6efd;
  }
</style>

<style>
  /* Set color of text in navbar */
  body {
        margin: 0;
    }

  .navbar {
    background-color: white;/*rgb(20, 97, 180);*/
    color: #fff;
    height: 70px;
    display: flex;
    justify-content: center;
    justify-content: flex-start;
    align-items: center;
  }

  .navlink {
    color: white;
    font-size: 2rem;
    font-family: Helvetica;
    color: #0d6efd;
    text-decoration: none;
    margin: 0 30px;
  }

  .right {
    margin-left: auto;
  }
</style>
{% endblock %}

<nav class="navbar">
  <a class="navlink" href="{{ path('main_homepage') }}">
    CoRide
    <img src="" style="width: 160px;height: auto;" class="main">
  </a>

  <a href="#" class="navlink">About</a>
  <a href="{{ path('app_trajet') }}" class="navlink">Poste</a>
  <a href="#" class="navlink">Trajet</a>
  <div class="right">
    {% if cp.current_user %}
      <a href="{{ path('sign-out') }}" class="navlink">Sign Out</a>
    {% else %}
      <a href="{{ path('log-in') }}" class="navlink">Log In</a>
      <a href="{{ path('sign-up') }}" class="navlink">Sign Up</a>
    {% endif %}
  </div>
  <div style="width: 100%; height: 3px; background-color: #0d6efd;"></div>

</nav>